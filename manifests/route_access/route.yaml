apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: test-route
spec:
  to:
    kind: Service
    name: test-route-service
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
