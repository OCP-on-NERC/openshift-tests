apiVersion: v1
kind: Pod
metadata:
  name: test-registry-pull-pod
spec:
  containers:
  - name: test
    image: ${INTERNAL_IMAGE}
    command: ["sh", "-c", "echo 'Image pulled successfully' && sleep 10"]
  restartPolicy: Never
